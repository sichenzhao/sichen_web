///|
let moonbit_learning_log : @model.Post = {
  slug: "moonbit-learning-log",
  date: "Draft",
  title: "MoonBit learning log",
  description: "Syntax notes, build commands, and small discoveries from learning the language.",
  body: [
    "My current workflow is deliberately short: edit the MoonBit source, run the build script, and refresh the static preview.",
    "The most useful lesson so far is to keep content types separate from rendering. MoonBit's package boundaries make that structure explicit and catch mistakes during the build.",
    "I will keep adding practical notes here as the site gains new pages and richer post formats.",
  ],
}
